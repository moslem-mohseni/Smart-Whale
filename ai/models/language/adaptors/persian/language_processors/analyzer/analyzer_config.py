# language_processors/analyzer/analyzer_config.py

"""
ماژول analyzer_config.py

این فایل حاوی تنظیمات و پارامترهای اصلی زیرسیستم آنالیز (analyzer) است.
سایر فایل‌های این زیرسیستم (مانند analyzer_data، analyzer_services، analyzer_processor و ...)
برای خواندن پارامترهای پیکربندی و آستانه‌های مهم به این ماژول مراجعه می‌کنند.

در صورت نیاز به تغییرات یا افزودن تنظیمات بیشتر، می‌توانید در این فایل آن‌ها را تعریف کنید.
"""

ANALYZER_CONFIG = {
    # آستانه شباهت کلی برای تشخیص و تحلیل
    "similarity_threshold": 0.8,

    # حداکثر تعداد تلاش برای برخی عملیات تحلیلی (در صورت نیاز)
    "max_retries": 3,

    # TTL کش برای نتایج آنالیز (بر حسب ثانیه)
    "analysis_cache_ttl": 3600,  # یک ساعت

    # حداقل امتیاز برای پذیرش نتیجه تحلیلی در بخش‌های مختلف
    "min_confidence_score": 0.5,

    # پارامترهای ترکیب امتیازات (وزن گرامر، وزن معنایی، وزن لهجه، ...)
    "analysis_weights": {
        "grammar": 0.3,
        "semantics": 0.4,
        "dialect": 0.2,
        "domain": 0.1
    },

    # سایر پارامترهای کنترلی که بسته به نیاز پروژه می‌توانند اضافه شوند
    # ...
}
