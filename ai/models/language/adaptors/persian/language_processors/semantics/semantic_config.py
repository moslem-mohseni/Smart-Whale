"""
تنظیمات اختصاصی ماژول semantics برای تحلیل معنایی متون فارسی
هدف: استفاده اولیه از مدل معلم به عنوان مدل مرجع و به تدریج انتقال یادگیری به مدل دانش‌آموز (smart model).
این تنظیمات شامل آستانه‌ها، پارامترهای مدل، TTL کش، ابعاد بردار معنایی و نرخ یادگیری مدل دانش‌آموز می‌باشد.
"""

CONFIG = {
    "confidence_threshold": 0.75,       # سطح اطمینان لازم برای استفاده از مدل‌های هوشمند
    "cache_ttl": 86400,                 # زمان TTL کش به ثانیه (24 ساعت)
    "vector_dim": 768,                  # ابعاد بردار معنایی
    "model_priority": {
        "teacher": True,                # در ابتدا مدل معلم به عنوان مدل اصلی مورد استفاده قرار می‌گیرد
        "smart": False                  # مدل دانش‌آموز در ابتدا غیرفعال است و با گذر زمان و یادگیری فعال می‌شود
    },
    "learning_rate": 0.01,              # نرخ یادگیری برای به‌روزرسانی مدل دانش‌آموز
    "update_frequency": 100,            # تعداد درخواست‌ها برای فعال‌سازی یادگیری مدل دانش‌آموز
    "fallback_vector": [0.0] * 768      # بردار پیش‌فرض در صورت عدم دسترسی به هیچ مدلی
}

if __name__ == "__main__":
    import json
    print("Semantic Module Configurations:")
    print(json.dumps(CONFIG, ensure_ascii=False, indent=2))
